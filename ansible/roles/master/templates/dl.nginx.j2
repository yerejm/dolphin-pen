server {
    listen 443 ssl;
    server_name {{ dl_server_name }};

    access_log logs/dl.access.log main;

    location / {
        root {{ buildmaster_builds_location }};
        autoindex on;
    }
}
