deb {{ debian_mirror }} {{ debian_release }} main
deb-src {{ debian_mirror }} {{ debian_release }} main

deb {{ debian_security_mirror }} {{ debian_release }}/updates main contrib
deb-src {{ debian_security_mirror }} {{ debian_release }}/updates main contrib

deb {{ debian_mirror }} {{ debian_release }}-updates main contrib
deb-src {{ debian_mirror }} {{ debian_release }}-updates main contrib

