---
- hosts: buildslaves
  user: root
  roles:
    - common
    - { role: buildbot, when: "ansible_os_family != 'Windows'" }
    - { role: builder, when: "ansible_os_family != 'Windows'" }
    - { role: slave, when: "ansible_os_family != 'Windows'" }
    - { role: windows, when: "ansible_os_family == 'Windows'" }

