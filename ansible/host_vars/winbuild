---
slave_username: windows
slave_password: windows

# The buildbot info are not used on windows due to ansible not having any
# facility to dynamically set the contents of these files. Since they are not
# important for slave operation, they are not important enough to define static
# files as a workaround.
buildbot_info_host: windows buildslave
buildbot_info_admin: windows@winbuild.dev

